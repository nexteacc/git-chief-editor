# Today Git Chief Editor Server Environment
# 复制此文件为 .env 并填入实际值

# ========== 必填 ==========

# Gemini API Key (用于 AI 报告生成)
GEMINI_API_KEY=your_gemini_api_key_here

# GitHub OAuth App (用于用户登录)
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# Session 密钥 (生产环境必须设置强随机字符串)
SESSION_SECRET=your_random_session_secret_at_least_32_chars

# ========== 可选 ==========

# GitHub OAuth 回调地址 (默认: http://localhost:3001/api/auth/callback)
# 生产环境需要改为实际域名，如: https://your-app.fly.dev/api/auth/callback
GITHUB_CALLBACK_URL=http://localhost:3001/api/auth/callback

# 前端地址 (开发环境用，生产环境不需要)
FRONTEND_URL=http://localhost:3000

# 服务端口 (默认: 3001，Fly.io 使用 8080)
PORT=3001

# 环境 (development/production)
NODE_ENV=development
